#pragma once


template<typename T>
int quick(T, int first, int second);

template<typename T>
int heap(T *array, int ind_l);

template<typename T>
int shell(T *array, int ind_l, int exp);

template<typename T>
void transp(T *array, int first, int second);